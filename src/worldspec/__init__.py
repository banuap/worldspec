"""WorldSpec — a declarative language and compiler for modeling enterprise
systems as entities, relationships, states, invariants, actions, and
transitions.

Versions are tracked independently (operating standards: versioning):
"""

LANGUAGE_VERSION = "0.1.0"
COMPILER_VERSION = "0.1.0"
IR_VERSION = "0.1.0"
PACKAGE_FORMAT_VERSION = "0.1.0"

__version__ = COMPILER_VERSION
